import { useFetcher, useNavigate, useLocation } from "react-router"

import { Delete } from "lucide-react"

const ProjectDeleteButton = () => {
  return (
    <div>ProjectDeleteButton</div>
  )
}

export default ProjectDeleteButton